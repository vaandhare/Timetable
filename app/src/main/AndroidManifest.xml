<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.indekode.timetable">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera" > </uses-feature>
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="in.indekode.timetable.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="in.indekode.timetable.MainActivity" />
        <activity android:name="in.indekode.timetable.main.HomeActivity" />
        <activity android:name="in.indekode.timetable.main.ProfileActivity" />
        <activity android:name="in.indekode.timetable.main.TimetableActivity" />
        <activity android:name="in.indekode.timetable.login.EditProfileActivity" />
        <activity android:name="in.indekode.timetable.login.LoginActivity" />
        <activity android:name="in.indekode.timetable.login.SignUpActivity" />
        <activity android:name="in.indekode.timetable.login.passwordActivity"></activity>


    </application>

</manifest>